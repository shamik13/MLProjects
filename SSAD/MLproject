name: ssad

docker_env:
    image: ssad
    volumes: ["/home/inoue/data:/mlflow/data"]

entry_points:
    dvc:
        parameters:
            data_dir: /mlflow/data
            branch: master
            commit_id:
        command: "bash dvc.sh --data_dir {data_dir} --branch {branch} --commit_id {commit_id}
    train:
        command: "python run.py conf/config.yaml"
